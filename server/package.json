{
  "name": "sidequest-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sidequest LiveKit backend: token minting, explicit agent dispatch, GPS plumbing.",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "node --env-file=../.env.local src/server.js",
    "test": "node --test \"test/*.test.js\"",
    "smoke": "node --env-file=../.env.local scripts/smoke.js",
    "llm:smoke": "node --env-file=../.env.local scripts/llm-smoke.js"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.13.43",
    "livekit-client": "^2.20.1",
    "livekit-server-sdk": "2.17.0"
  }
}
